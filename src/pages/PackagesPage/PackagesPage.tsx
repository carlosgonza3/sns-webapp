import { PackagesHeroSection } from '@/sections/packages/PackagesHeroSection/PackagesHeroSection';

export default function PackagesPage() {
    return (
        <main id="main-content">
            <PackagesHeroSection />
        </main>
    );
}
